# Rime/Weasel (速倉) 2026-06-07 重構優化記錄

本文件記錄了針對速倉輸入法「智慧字懶加載」、「45字候選上限與補滿」及「字根差分桶遞增模型」的重構歷程。

---

## 📝 懶加載與字根差分桶優化重構記錄

本輪重構解決了過濾器中「候選字數強截斷」、「智慧調頻失效」與「短碼輸入時長編碼補全遮擋單字」的三大痛點。

### 1. 實現真正的 3-Tier 懶加載 (Lazy Loading)
* **病因**：舊版本以 `break` 強制在 `45` 個候選項時中斷，導致物理截斷，後半段字典的候選字永遠無法被打出。
* **解決方案**：
  * 設定 `sort_threshold = 20`。
  * 僅對前 20 個非丟棄候選字進行緩存、三層分類與加權排序，並流式輸出。
  * 第 21 個候選字之後恢復原生流式輸出（直接 `yield(cand)`），不作緩存與二次排序，消除卡頓。
  * 實裝 strict 45 字限制。當 `total_yielded >= 45` 時立刻中斷遍歷。若 `tier1 + tier2` 不足 45 個，則在輸出完後，動態載入 `tier3` 的字（英文、生僻字、聯想句）補滿到 45 個。

### 2. 實裝字根差分桶遞增模型 (Key Length Incremental Model)
* **病因**：輸入短碼（如 `qn`）時，字根匹配長度較長之補全詞（如 `打開` 4碼）會因為原始權重高而遮擋精確匹配之短碼字（如 `打` 2碼）。
* **解決方案**：
  * 計算候選字匹配編碼長度與當前輸入字根長度的差值：`len_diff = math.max(0, code_len - input_len)`。
  * 排序優先權：在 `compare_tier2` 中，優先依照 `len_diff` 由小到大排序（即 `len_diff = 0` 的精確匹配最優先，隨後是補全 `1 碼`、`2 碼` 等）。
  * 在同一個 `len_diff` 分桶內部，再依照自適應分數（包含動態智慧調頻）進行排序。

## ❌ 失敗歷程與待解決問題 (2026-06-07)

### 1. 「帳密」(lvju) 精確匹配置頂失敗
* **現象**：儘管實裝了 `len_diff = math.abs(code_len - input_len)` 與 `user_table` 類型提升，智慧詞「帳密」在輸入 `lvju` 時仍排在第 3 位，位於字典單字「中」(l)、「帳」(lv) 之後。
* **診斷推論**：
  * **猜測 A**：`word_code` 提取失敗。若 `cand.comment` 無法正確提取出字母編碼，`len_diff` 可能計算錯誤。
  * **猜測 B**：`quality` 分數差距過大。若字典詞的原始品質遠超智慧詞，且 `len_diff` 因某種原因未能生效，則排序仍會退回分數比對。
  * **猜測 C**：Lua 緩存或環境變數問題。`os.getenv("APPDATA")` 在特定環境下可能指向錯誤，導致字典載入失敗。

### 2. 「yrs」候選詞集體降級 (Tier 2 消失)
* **現象**：輸入 `yrs` 時，首位為「誠」(yrs)，隨後全是聯想詞（Tier 3），而字典中原本應屬 Tier 2 的「訪」、「誇」、「詐」、「誘」完全沒有出現在前列。
* **診斷推論**：
  * **核心懷疑**：`dict_entries` 載入不全或路徑異常。如果 `sucang.dict.yaml` 載入失敗，所有字典詞會因為 `dict_info == nil` 被降級至 Tier 3，失去排序優勢。
  * **路徑問題**：腳本中 `os.getenv("APPDATA")` 拼接的路徑可能與 Rime 實際執行時的路徑不符。

## ✅ 2026-06-07 v22 最終成功方案：連打優先與生字隔離排序

### 1. 成功邏輯摘要
* **意圖導向分層**：
  * **Tier 2 (核心區)**：字典詞 (`table`)、已選詞 (`user_table`) 與 **短造句 (2-3字的 `sentence`)**。將原本 0 分的「帳密」等連打詞拉回核心競爭區。
  * **Tier 3 (隔離區)**：**生字/長碼補全 (`completion`)** 與 4字以上長句子。徹底解決 `𥠨` 或 `記(yrsu)` 插隊問題。
* **動態長度優先權**：
  * **輸入 >= 3碼**：2字詞 > 1字詞 > 其他。確保 `lvju` 下「帳密」置頂。
  * **輸入 <= 2碼**：1字詞 > 2字詞。確保基礎字典單字功能。

### 2. 最終結果驗證
* `lvju`：帳密 > 中/帳 > 史瓦辛格。 (成功)
* `hvni`：很多 > 𥠨。 (成功)
* `yrs`：誠/訪 > 記/遐 > 調研報告。 (成功)
* `dm`：單字優先。 (成功)

### 2. 技術盲點分析 (Post-mortem)
* **身分誤判**：誤將 Rime 的聯想詞（如史瓦辛格）判定為 `len_diff = 0` 或 `user_table`，導致其獲得 Tier 2 優先權並以高 `quality` 壓制真正精確匹配的詞。
* **字典與環境依賴**：Lua 腳本中的字典載入邏輯在不同環境下極不穩定，導致 `yrs` 依賴的 `dict_info` 經常失效，造成字典詞集體降級。
* **分層設計缺陷**：過於依賴 `len_diff` 運算而忽略了 Rime 原生品質分數的權重，且對「已學習」與「預測補全」的標籤定義理解不夠精確。
* **Hardcode 陷阱**：嘗試使用 `+5000` 等魔法數字進行 Ad-hoc 修正，反而破壞了整體排序的線性度，導致如「史瓦辛格」等長詞在不當加權下竄升。

### 3. 結論與回滾
* **行動**：已將 `lua/filter_sentence_restriction.lua` 回滾至 Commit `8a01a2b`。
* **教訓**：在未徹底釐清 Rime 翻譯器輸出標籤（Type）與品質分（Quality）的真實映射前，不應嘗試大規模重構排序引擎。

---

## ❌ 2026-06-07 v27-v28 失敗詳解：高壓掃描與路徑陷阱

### 1. v27：暴力掃描導致的性能崩潰 (High-Prefetch Trap)
* **策略**：將預讀上限提升至 `5000+`，企圖暴力搜尋被 `initials` 淹沒的低權重字根（如 `密` ju）。
* **失敗原因**：嚴重違背「懶加載」原則。Rime 為單執行緒，每按一個鍵都要遍歷 5000 個候選字會造成：
  * **極度卡頓**：輸入反應遲緩超過 1 秒。
  * **引擎當機**：在 Windows WeaselServer 下可能導致程序無回應。
* **教訓**：在 Rime 這種環境，**「量」永遠無法解決「序」的問題**。

### 2. v28：字典依賴與類型判斷失誤 (Path & Logic Trap)
* **策略**：嘗試實裝目標驅動截斷 (Target-Driven Cutoff)，但高度依賴 `sucang.dict.yaml` 的外部載入。
* **失敗原因**：
  * **路徑不可信**：`os.getenv("APPDATA")` 與 `working_dir` 在不同 Rime 接口下表現不一。一旦字典載入失敗，所有精確匹配判定歸零。
  * **類型優先級錯誤**：誤將 `sentence` (智慧造句) 與 `completion` (聯想詞) 混為一談，導致「帳密」排在「史瓦辛格」之後。
* **教訓**：Lua 過濾器應儘可能保持 **Stateless (無狀態)**。

---

## ✅ 2026-06-07 v29 終極成功方案：精確匹配與連打優先的完美平衡

### 1. 成功邏輯摘要 (Breakthrough)
* **去字典依賴化 (Stateless Detection)**：
  * **痛點**：Lua 讀取 `sucang.dict.yaml` 耗時且路徑在不同 OS 下極不穩定，一旦讀取失敗，所有排序邏輯崩潰。
  * **解決方案**：直接偵測 Rime 傳出的 `cand.comment`。若註釋中的字母編碼與 `env.engine.context.input` 完全一致，即判定為 **Exact Match (精確匹配)**。
* **精確匹配絕對權威 (Exact-First)**：
  * 將精確匹配字（如 `竹`、`德`、`密`、`了`）提升至 Tier 2 的最高層級。
  * **規則**：精確匹配字 > 補全詞/聯想詞。這徹底解決了字根被「史瓦辛格」或「了了」淹沒的問題。
* **適應性連打優先 (Contextual Phrase-First)**：
  * 在同為 Tier 2 的內部：
    * **輸入 >= 3 碼**：2 字詞 (如 `帳密`) > 1 字詞。
    * **輸入 <= 2 碼**：1 字詞 > 2 字詞。
  * 這兼顧了速成單字效率與長輸入的造句需求。
* **目標驅動懶加載 (Target-Driven Lazy Loading)**：
  * **規範**：不再設定幾百幾千的盲目預讀量，而是只要 Tier 1 + Tier 2 湊滿 **45 個** 有效候選字（足夠填充 5 頁）就立刻停止迭代。
  * **結果**：在開啟 `initials` (簡拼) 導致候選字爆炸的情況下，依然保持 0 延遲的流暢感。

### 2. 最終驗證結果
* `lvju`：帳密 > 帳 > 史瓦辛格。 (成功)
* `h`：竹 > 是 > 的。 (成功)
* `hp`：德/怎 > 第一階段。 (成功)
* `nn`：了 > 了了。 (成功)
* `oam`：但 > 會更。 (成功)

### 3. 核心教訓 (Key Lessons)
1. **不要挑戰 Rime 的性能極限**：預讀超過 1000 次就會感到延遲，5000 次會導致部署崩潰。目標驅動的動態截斷才是正解。
2. **Comment 是最好的身分證**：與其寫複雜的 Lua 字典解析器，不如直接解析 Rime 已經幫你算好的 `cand.comment`。
3. **分層優於加權**：使用 Tier 區隔「精確」與「模糊」比調整品質分數更穩定。

---

## ✅ 2026-06-08 v36.2 終極成功方案：多碼單字保護與物理隔離

### 1. 成功邏輯摘要 (Final Breakthrough)
*   **物理隔離聯想句 (Sentence Isolation)**：
    *   **病因**：`sucang_sentence` 品質分 50 導致其在 2000 次迭代內淹沒了品質分 33 的單字，觸發過早截斷。
    *   **解決方案**：在 `sucang.custom.yaml` 將聯想句品質分降至 `1`。
*   **強化無狀態偵測 (Enhanced Stateless Detection)**：
    *   **病因 (v36.1 殘留)**：多碼單字（如 `變` vfk/vk）在無註釋回退查找時，會查到較長的基準碼 (vfk, len 3)，導致輸入 `vk` 時 `len_diff` 變為 1 而降級。
    *   **解決方案**：主動查找 `dict_entries[text .. "_" .. input_str]`。只要字典支持當前輸入碼，強制判定為 `is_exact` (len_diff = 0)。
*   **精準中斷機制 (Refined Cutoff)**：
    *   **規則**：只有真正精確匹配 (`is_exact`) 或 `n+1` 補全才計入 54 個中斷名額。長碼補全不再擠佔名額，確保遍歷窗口能捕捉到所有單字。
*   **掃描窗口擴張**：將 `iterated` 上限從 2000 提升至 3000。

### 2. 最終驗證結果
*   `rh`：呀 > 吵 > 吻 > 踢 (成功，單字全數回歸)
*   `nt`：登 > 孟 > 盈 > 阱 (成功)
*   `vk`：變 > 收 > 繳 > 嫉 (成功，常用字重回首位)

### 3. 核心教訓
1.  **變數順序是生命線**：在 Lua 迴圈中，必須先提取候選字屬性，再進行邏輯判定（修復了 v36.1 的 nil 崩潰）。
2.  **字典不只是回退工具**：在處理多碼字時，必須進行「字+碼」組合查找，才能對抗主編碼長度造成的位移。


